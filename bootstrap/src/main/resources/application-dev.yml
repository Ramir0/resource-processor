services:
  resource-service:
    url: http://localhost:8081
  song-service:
    url: http://localhost:8082

spring:
  config:
    import: optional:configserver:http://localhost:8090
  rabbitmq:
    host: localhost
    username: guest
    password: guest
